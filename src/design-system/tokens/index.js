// ═══════════════════════════════════════════════════════════════════════
// 🎨 Design System - Main Token Export
// ═══════════════════════════════════════════════════════════════════════

import colors, { gradients } from './colors';
import typography from './typography';
import { spacing, borderRadius, shadows, zIndex } from './spacing';
import animations from './animations';

export const tokens = {
  colors,
  gradients,
  typography,
  spacing,
  borderRadius,
  shadows,
  zIndex,
  animations,
};

export {
  colors,
  gradients,
  typography,
  spacing,
  borderRadius,
  shadows,
  zIndex,
  animations,
};

export default tokens;
